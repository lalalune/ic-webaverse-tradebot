import { Types } from "../config";

export const playerItemSlots = {
  all: [Types.HEAD, Types.CHEST, Types.PANTS, Types.FEET, Types.ICTOKEN]
};

export const remoteItemSlots = {
  all: [Types.HEAD2, Types.CHEST2, Types.PANTS2, Types.FEET2, Types.ICTOKEN]
};